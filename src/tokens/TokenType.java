package tokens;

/**
 * The types of Token that can be placed by a Player. Used to identify Tokens on the board that
 * belong to a Player with the same type.
 */
public enum TokenType {
  WHITE,
  BLACK
}
